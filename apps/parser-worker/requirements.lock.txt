# parser-worker direct + tooling pins.
#
# Source of truth for ranges is pyproject.toml [project].dependencies.
# This lock is a reproducible pin of the direct (and first-level runtime) deps
# verified against the local development interpreter (CPython 3.14 user
# site-packages) on 2026-09-22. There is no uv.lock here on purpose: the
# deployment image installs from this file with
#   pip install -r requirements.lock.txt
# and the hatchling wheel metadata still governs library consumers.
#
# Regenerate after intentional upgrades:
#   python3 -m pip install --user -e '.[pdf-render]' pytest ruff mypy
#   python3 -m pip freeze --user | grep -E '^(fastapi|uvicorn|pydantic|...)=...'
#   # or, inside a clean venv: pip install -e . && pip freeze > requirements.lock.txt

# --- runtime (pyproject [project].dependencies) ---
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
python-multipart==0.0.32
httpx==0.28.1
pypdf==6.16.2
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5
xlrd==2.0.2

# --- first-level runtime deps pinned for reproducibility ---
starlette==1.6.0
anyio==4.13.0
httpcore==1.0.9
h11==0.16.0
certifi==2026.1.4
idna==3.11
sniffio==1.3.1
annotated-types==0.7.0
pydantic-core==2.46.4
typing-extensions==4.15.0
et-xmlfile==2.0.0
lxml==6.1.1
Pillow==12.1.1

# --- operational peer used by the API/worker for S3-compatible object storage ---
# (CI installs it today; kept here so local/CI images match.)
minio==7.2.20

# --- optional extras (pyproject [project.optional-dependencies]) ---
# pdf-render: pymupdf>=1.24.0   # install with: pip install -r requirements.lock.txt 'pymupdf>=1.24.0'
# layout-models: docling>=1.1.0 # install with: pip install -r requirements.lock.txt 'docling>=1.1.0'

# --- test / lint toolchain (not runtime) ---
pytest==9.1.1
ruff==0.14.5
mypy==1.18.2
