[Unit]
Description=LLMWiki Parser Worker
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/gbrainkg/apps/parser-worker
EnvironmentFile=%h/gbrainkg/apps/api/.env
ExecStart=/usr/bin/python3 -m uvicorn src.main:app --host 127.0.0.1 --port 8100
Restart=always
RestartSec=5
TimeoutStopSec=15

# Limits and Logging
MemoryMax=4G
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=default.target
